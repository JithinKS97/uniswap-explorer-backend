# Uniswap explorer backend 
### Steps to run the service
1. Install the npm dependencies
2. Create the env file and fill the values
3. Run `npm run start`
4. The server will run at port 8000 by default